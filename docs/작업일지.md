# 마이풉💩 작업일지

> 인간 협업자와 AI 협업자가 함께 기록하는 프로젝트 작업 로그
> 
> **작성 규칙**: 날짜별로 기록하고, 누가 작성했는지 표시해주세요.
> 작성자 태그: `[인간]` `[AI]` `[함께]`

---

## 2026-04-22 (화)

### 한 일
- `[함께]` MyPoopAI MVP 모바일 웹앱 프로젝트 생성 (React + Vite + Firebase)
- `[AI]` 전체 앱 구조 설계 및 6개 화면 구현
  - 온보딩 (3단계 슬라이드)
  - 홈 (성인/아기 모드 전환, 주간 건강 요약)
  - 카메라 촬영 (모자이크/스티커 오버레이 실시간 적용)
  - AI 분석 결과 (Bristol Stool Scale 기반, 별점 0~5 / 0.5단위)
  - 분석 히스토리 (날짜별 그룹핑)
  - 설정 (오버레이 타입 토글, 모드 전환, 로그아웃)
- `[AI]` 디자인 시스템 적용 — 컬러팔레트(라벤더 #B8A9C9, 베이지 #D4B896), Pretendard 폰트
- `[AI]` PWA 설정 (manifest.json, 모바일 메타태그)
- `[AI]` Mock AI 분석 서비스 구현 (Bristol 7단계 + 색상 분석, 성인/아기 모드 분리)

### 문제점 / 이슈
- Firebase 실제 연동 전 — 현재 demo 키로 세팅되어 있음. Firebase Console에서 프로젝트 생성 후 `.env` 파일에 실제 키 입력 필요
- AI 분석이 Mock 상태 — 실제 Vision AI 모델 연동 필요 (Google Cloud Vision API 또는 자체 모델)
- 앱 아이콘/로고 미제작 — 일러스트레이터로 캐릭터 및 아이콘 디자인 필요
- 스티커 오버레이가 이모지(💩) 기반 — 오리지널 캐릭터 에셋으로 교체 예정

### 다음에 할 일
- [ ] Firebase 프로젝트 생성 및 실제 키 연동
- [ ] Google Auth 로그인 연동
- [ ] Figma에서 UI/UX 디자인 정리
- [ ] 일러스트레이터로 마이풉 캐릭터 / 스티커 에셋 제작
- [ ] 실제 AI 분석 모델 선정 및 연동
- [ ] Firebase Hosting 배포 + mypoopai.com 도메인 연결

### 메모
- 기술 스택 확정: React + Vite + Firebase (Auth, Firestore, Storage, Hosting)
- 기획서 v0.2 기반으로 작업. 기획서 원본 파일은 이전 세션에서 작성됨

---

## 2026-04-22 (화) — 오후 세션

### 한 일
- `[함께]` GitHub 리포지토리 정리 — 기존 `mypoopai` 삭제 후 `MyPoopAI`로 새로 생성
- `[AI]` 폴더 구조 정리 — 중복 폴더(`src/src/`, `public/public/`, `mypoopai/` 하위) 제거, 루트에 깔끔하게 재배치
- `[함께]` 2-폴더 동기화 시스템 구축
  - 폴더 1 (`바탕 화면\myPoopAI`) — Cowork 작업용 (롤백 겸용)
  - 폴더 2 (`mypoopai-reorg`) — Claude Code 전용 작업 폴더
  - `start-sync.bat` / `end-sync.bat` 스크립트 생성 (`C:\Users\thefl\scripts\`)
  - Claude Code `settings.json`에 SessionStart/SessionEnd hooks 등록
- `[AI]` `.gitignore` 강화 — Cowork 전용 파일 패턴 추가 (이미지 작업파일, 암호/키, OS/IDE 캐시)
- `[함께]` 폴더 1에 Git 저장소 초기화 및 remote 연결

### 문제점 / 이슈
- `gh` CLI가 Windows에 미설치 → GitHub API + PAT 토큰으로 리포 생성 처리
- PAT 토큰이 대화에 노출됨 → 토큰 삭제 필요 (https://github.com/settings/tokens)
- 폴더 1에 중복 폴더/파일이 남아있었으나 수동 삭제 완료

### 다음에 할 일
- [ ] PAT 토큰 만료/삭제 확인
- [ ] `gh` CLI 설치 (선택)
- [ ] Firebase 프로젝트 생성 및 실제 키 연동
- [ ] Google Auth 로그인 연동
- [ ] Figma에서 UI/UX 디자인 정리

### 메모
- 작업 규칙: 폴더 1은 명시적 지시 없으면 Claude Code가 건드리지 않음 (롤백용)
- 동기화 흐름: 세션 시작 → 폴더1 push → 폴더2 pull / 세션 종료 → 폴더2 push → 폴더1 pull

---

## 2026-04-22 (화) — 저녁 세션

### 한 일
- `[AI]` Phase 1 MVP 전체 개발 계획 수립 및 문서화
  - `docs/개발계획_Phase1_MVP.md` 작성 (Sprint 0~7 구조)
  - 7개 스프린트, 약 28시간 분량 로드맵
- `[AI]` Firebase Auth 4종 인증 연동 완성
  - 이메일/비밀번호 (기존 유지)
  - Google OAuth (기존 유지)
  - Apple OAuth (기존 유지)
  - 카카오 로그인 신규 추가 (Kakao JS SDK + Firebase 연동)
- `[AI]` AuthModal에 카카오 버튼 UI 추가 (카카오 노란색 #FEE500)
- `[AI]` AuthContext에 loginWithKakao 액션 추가
- `[AI]` SettingsPage LOGIN_TYPE_LABELS에 kakao 추가
- `[함께]` Node.js v14 → v22.22.2 업그레이드 (nvm use 22.22.2)
  - Vite 8 빌드 오류 해결 (`??=` 연산자 미지원 문제)

### 문제점 / 이슈
- Node.js v14에서 Vite 8 빌드 불가 — v22로 업그레이드하여 해결
- 카카오 로그인: Firebase 네이티브 미지원 → 카카오 SDK + 이메일 기반 계정 생성 방식으로 MVP 구현
  - 향후 Firebase Cloud Functions로 커스텀 토큰 방식으로 개선 필요
- 카카오 게스트→계정연결(upgrade)은 MVP에서 미지원 (이메일/Google로 안내)

### 다음에 할 일
- [ ] Firebase Console에서 Auth 제공자 활성화 (Google, 이메일)
- [ ] 카카오 개발자 앱 생성 및 JS 키 발급 → `.env`에 `VITE_KAKAO_JS_KEY` 설정
- [ ] Apple Developer 설정 (Sign in with Apple)
- [ ] 네비게이션 3탭 구조 변경 (Sprint 1)
- [ ] Claude Vision AI 실제 연동 (Sprint 2)
- [ ] PWA 아이콘 + 서비스워커 (Sprint 3)
- [ ] Firebase Hosting 배포 (Sprint 6)

### 메모
- Node.js 22.22.2 (LTS) 사용 중. `nvm use 22.22.2`로 전환
- 카카오 SDK CDN: `https://t1.kakaocdn.net/kakao_js_sdk/2.7.4/kakao.min.js`
- 전체 개발 계획은 `docs/개발계획_Phase1_MVP.md` 참고

---

<!-- 아래 템플릿을 복사해서 새 날짜를 추가하세요 -->

<!--
## YYYY-MM-DD (요일)

### 한 일
- `[작성자]` 작업 내용

### 문제점 / 이슈
- 내용

### 다음에 할 일
- [ ] 내용

### 메모
- 내용
-->
